[package]
name = "tinybuild"
version = "0.1.0"
edition = "2021"
description = "A tiny build system that turns annotated shell scripts into a cacheable, parallel DAG."
license = "MIT"

[[bin]]
name = "tinybuild"
path = "src/main.rs"

[dependencies]
glob = "0.3"
sha2 = "0.10"
